import styled from '@emotion/styled';

export const Head = styled.div`
  padding: 6px 0;
  color: #fff;

  h2 {
    text-overflow: ellipsis;
    white-space: nowrap;
    display: block;
    overflow: hidden;
    font-size: 18px;
    font-weight: 900;
  }
`;
